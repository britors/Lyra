# Lyra OS — gaming/system tuning (§8)
# Many Proton/Wine titles (esp. via DXVK/VKD3D and esync) need a high mmap count
# and they crash with the default 65530.
vm.max_map_count = 2147483642

# Better desktop responsiveness under memory pressure.
vm.swappiness = 10
