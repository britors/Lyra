# Lyra OS — LIVE-ONLY SDDM autologin.
# Removed from the installed system by the Calamares lyra-live-cleanup step (§11).
[Autologin]
User=lyra
Session=plasma
Relogin=false

[General]
# Live session uses Wayland by default (spec §2); X11 fallback remains selectable
# from the SDDM session menu after Calamares installs to disk.
DisplayServer=wayland
