# Lyra OS — run the GPU driver installer in the target chroot (§6)
---
dontChroot: false
timeout: 1200      # DKMS builds can take a while on slow machines
script:
  - "/usr/local/bin/lyra-gpu-install"
