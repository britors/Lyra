# Lyra OS — set Lyra as the default Plymouth theme on install/upgrade (§10).
post_install() {
    if command -v plymouth-set-default-theme >/dev/null 2>&1; then
        plymouth-set-default-theme -R lyra || true
    fi
}

post_upgrade() {
    post_install
}
