[main]
# Explicit `zypper refresh` is unaffected. Automatic checks remain fresh
# enough for interactive use without contacting every repository on nearly
# every package command; Vega performs its authoritative check every 4 hours.
repo.refresh.delay = 60
