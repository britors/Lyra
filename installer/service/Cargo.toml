[package]
name = "lyra-installer-service"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Privileged backend for the Lyra OS installer, launched via pkexec"

[dependencies]
lyra-installer-core = { package = "lyra-installer-core", path = ".." }
serde_json = "1"
